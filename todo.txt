https://stackoverflow.com/questions/18660433/matlab-mex-file-with-mexcallmatlab-is-almost-300-times-slower-than-the-correspon
site with a possible solution to pass a function_handle as an input (to test if performances are better than evalString)

set matlab path automatically

tracking C++ and Matlab memory leaks


the path for the file command1.m depends on from which directory matlab is opened

matlab interface : quit temporalrly vs quit progmram
enregistrer l'image finale de la solution dans un fichier

dans les options : save image + laisser utilisateur entrer un nom
for MMU, test if the sizeMesh correctly moves with the solution

virer tous les chemins absolus (car changeant d un utilisateur à l'autre)
think about parameter tau

destructors

portability of the code, makefile
documentation of the code

prove that GaussThomasAlgo is stable in our case( dominant matrix)

6)keeping an history of previous commands ( in the case a typing error occurs)

destroy arrays in engine


http://pure.iiasa.ac.at/10737/1/IR-13-023.pdf to keep for the report

--------------Parameter stuffs --------------
specify parameters
making that if lengthAtBirth > maximumLength : return errors
replacing the addParameters etc in model by a container

